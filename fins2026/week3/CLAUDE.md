## Weekly Overlay

Read `AGENTS.md` in this week folder first.
Then load `APP_LAB.md`, `APP_AUDIT.md`, `app/README.md`, and
`guidance/output-context.md`.
Keep edits inside this week unless a shared utility belongs in `fintools/`.

